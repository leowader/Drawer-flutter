import 'package:flutter/material.dart';

class Salvar extends StatelessWidget {
  const Salvar({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("ESTAS EN SALVAR"),
    );
  }
}